function calcular_media(n1, n2) {
    const media = (n1 + n2) / 2
    return media
}

const aluno = "Arthur Moreto"
const rs = calcular_media(4.5, 8.9)

if (media => 6) {
    console.log(`O aluno ${aluno} está aprovado com média de ${rs}`)
} else {
    console.log(`O aluno ${aluno} está reprovado com média de ${rs}`)
}