const bairros = ["Centro", "Vila Fátima", "Jaraguá", "Artemis", "Cecap",
     "Vale do Sol", "Vila Industrial", "Mário Dedini", "Ondas", "Paulista"]



for (let index = 0; index < bairros.length; index++) {
    const bairro = bairros[index];
    console.log(`O bairro é ${bairro}`)   
}